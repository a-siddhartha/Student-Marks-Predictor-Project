cmd in windows file explorer path of folder (basically click on the name of the folder when inside it)

pip install -r requirements.txt




(only if needed, read which packages have error)

pip install --upgrade pandas numpy matplotlib seaborn scikit-learn

pip install --upgrade -r requirements.txt


(To run in cmd)
streamlit run Main.py

copy link from cmd and paste in browser
